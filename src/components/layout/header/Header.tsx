import HeaderContent from './header-content/HeaderContent';

const Header: React.FC = () => {
  return (
    <header className='text-white page-bg-color'>
      <HeaderContent />
    </header>
  );
};

export default Header;
