import MobileNavigation from '@/components/screens/mobile-navigation/MobileNavigation';
import { NextPage } from 'next';

const NavigationPage: NextPage = () => {
  return <MobileNavigation />;
};

export default NavigationPage;
